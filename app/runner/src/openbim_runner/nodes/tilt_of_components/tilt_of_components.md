# Node Development Prompt (RCC4ALL) — tilt_of_components

This file is the persistent memory for the **tilt_of_components** node. Read it
before starting any session on this node.

## Node identity
- **Name (registry key):** `tilt_of_components`
- **Category:** `geometry`
- **Purpose:** Measure the tilt of IFC building components relative to the
  horizontal plane and flag components outside a configured comparison threshold.

## Decided architecture (locked with user)
1. **Manual element-category selector** setting: `2d` (walls & slabs) vs `1d`
   (columns & beams) — picks the algorithm.
2. **Slim structured checks per element** (patterned on `property_comparison`),
   NO human-only-message result and NO spreadsheet export.
3. **Keep the 4-option comparison enum** + limit fields, tolerance
   default `0.1°`.
4. **Basic path only for v1:** two-biggest-surfaces (2D) / farthest-centroids (1D).
   Openings/VOIDS handling **deferred** (still works for walls with windows because
   the hole is already subtracted from the wall's mesh).
5. **Emit helper geometry** into the geometry cache (surface mesh 2D / thin axis
   cylinder 1D).

## Tilt semantics (important)
The tilt value is the **smaller angle between the element's dominant surface/axis
and the horizontal plane**: vertical wall/column = `90°`, horizontal slab/beam =
`0°`. The tilt is computed as follows:
- 2D surface: mean of `acos(-normal_z)` over the surface's triangles (in degrees),
  complemented to `180 - x` when `> 90.1`, rounded to 2 dp.
- 1D: area-weighted surface centroids, farthest pair = axis; tilt =
  `|acos(dot(axis, NEG_Z)/|axis|) * 180/pi - 90|`, rounded 2 dp.

## Node specification
### Settings — `TiltOfComponentsSettings`
- `element_category: Literal["2d","1d"]` (default `2d`)
- `comparison_method: Literal["greater_than_lower","less_than_upper","inside_interval","outside_interval"]`
- `lower_limit: float = 0.0` (used by `greater_than_lower`)
- `upper_limit: float = 90.0` (used by `less_than_upper`)
- `interval_lower: float = 0.0` / `interval_upper: float = 90.0`
  (  one interval reused by both `inside_interval` / `outside_interval`, since only
  one method is active at a time)
- `horizontal_separation_angle: float = 5.0` (°; facet-merging for curved objects)
- `tolerance: float = 0.1` (°)

### Flag logic (tolerance applied; `passed = not flagged`)
- `greater_than_lower`: flag if `tilt > lower_limit + tol`
- `less_than_upper`: flag if `tilt < upper_limit - tol`
- `inside_interval`: flag if `tilt > interval_lower - tol and tilt < interval_upper + tol`
- `outside_interval`: flag if `tilt < interval_lower - tol or tilt > interval_upper + tol`

### Inputs
- `express_ids: list[int]` (from upstream `ifc_element_filter`). Empty → gather all
  `IfcElement` from the model via `context.ifc_model.by_type("IfcElement")`
  (consistent with `property_comparison`).

### Result (slim structured; mirrors `property_comparison`)
- `TiltSurfaceCheck`: `expected: str` (human-readable **pass** expectation, i.e. the
  inverse of the flag condition, combined from the comparison method and limits;
  `"less than or equal to X"`, `"greater than or equal to X"`, `"outside A and B"`,
  `"inside A and B"`; trailing `.0` trimmed; uses the **raw** configured limits,
  not the tolerance-adjusted boundary),
  `tilt_angle: float` (°), `passed: bool`, `geometry_key: str | None`
  (only for flagged).
- **No `message` field** and **no `condition` / `expected_min` / `expected_max`**
  — the combined `expected` string replaces them. Human-readable text is
  generated by the downstream `bcf_export` node (consistent with
  `property_comparison`'s pure-logic design); the raw comparison method is not
  emitted (dropped by user decision, shared with the `expected` wording).
- `TiltsElement`: `express_id: int`, `class_name: str` (IFC class or `unknown`),
  `element_category`, `failed: bool`, `checks: list[TiltSurfaceCheck]`
- `TiltOfComponentsResult`: `element_count`, `check_count` (element-level:
  one per element with ≥1 check; skipped no-geometry elements not counted),
  `failed_count` (element-level: one per element with ≥1 flagged surface/axis),
  `model_name` (node-local, from the IFC header file name basename; falls back
  to `IfcProject` name, then `""`), `elements`
- 2D ⇒ up to 2 checks (front/back surfaces); 1D ⇒ 1 check (axis).
- Result shape mirrors the LOI/`property_comparison` check convention
  (`expected` / `tilt_angle`(=actual) / `passed`).

## Behavior / edge cases
- Element without tessellated geometry, or with a missing/invalid express_id →
  element with `class_name="unknown"`, empty checks, `failed=False`.
- **Composite / decomposed elements:** when an element has no own `Body` mesh but
  is decomposed via `IfcRelAggregates` into parts (e.g. multi-layer wall →
  `IfcBuildingElementPart`), its tilt is measured as a whole by merging the Body
  meshes of all its parts, recursing through the aggregation (`_resolve_composed_mesh`
  + `_collect_descendant_meshes`). Own `Body` takes precedence when present (no
  decomposition read). Parts remain listed & measured independently (keep-both).
  Depth-first with a `seen` set guards shared parts and decomposition cycles.
  This is buildingSMART-valid: `IfcBuildingElementPart` explicitly documents
  composite/sandwich walls modelled as per-layer elements.
- Validation `ValueError`: interval methods with `interval_lower > interval_upper`;
  `greater_than_lower` with negative `lower_limit`; `less_than_upper` with negative
  `upper_limit`; negative `horizontal_separation_angle`.
- `_group_surfaces` uses a grouping test: near-horizontal normals
  (`|z| < 0.99` and same vertical angle `< 0.001`) group by 2D horizontal angle
  `< horizontal_separation_angle`; otherwise group by 3D angle `< 1°`.
- Helper geometry keys: `inter:tilt_surface_{express_id}_{surface_index}` (2D),
  `inter:tilt_axis_{express_id}` (1D). `inter:` keys are excluded from collision
  inputs / whole-model fallback.

## Layout & registration
- Runner: `app/runner/src/openbim_runner/nodes/tilt_of_components/tilt_of_components.py`
- README.en.md / README.de.md (frontmatter `title`/`description`/`categories`).
- tests: `tests/test_tilt_of_components.py`
- Registered in `nodes/__init__.py` (import + `__all__`).
- Web: `app/web/app/nodes/TiltOfComponents/TiltOfComponents.vue` (+ `types.ts`).
- Registered in `app/web/app/utils/nodes.ts` (`tilt_of_components: 'TiltOfComponents'`).
- Schema regenerated via `npm run generate:schema` (Python is source of truth).

## UI — inline SVG tilt diagrams (web only)
- Two inline `<svg>` diagrams in `TiltOfComponents.vue`, always visible just below
  the Element category selector, switching on `element_category`:
  - **2D:** a wall with its front/back (two largest) surfaces highlighted; arc =
    tilt angle to horizontal.
  - **1D:** a column with its longitudinal axis highlighted; arc = tilt angle to
    horizontal.
- Angle representation = **to horizontal** (matches the runner's measured value: a
  vertical element reads 90°, not 0°).
- English caption under each image. Purely web UI: no runner/schema/nodes.ts changes
  (kept fully inside the node directory — no `public/` files).

## Verification (all passed)
- Runner (in `app/runner`): `uv run pytest` (147 passed, incl. 16 tilt tests),
  `uv run ruff check` (clean), `uv run ruff format --check` (clean),
  `uv run pyright` (0 errors).
- Web (in `app/web`): `npm run lint` (clean), `npm run typecheck` (clean),
  `npm run generate:schema` ✅ (node exported to schema.d.ts).

## Constraints
- Only modify files within/for this node + its two registration points. Never
  touch general/shared/unrelated files. No git operations.

## Open items
- Openings/VOIDS (`IfcRelVoidsElement`) surface selection not implemented (deferred).
- No xlsx/CSV export (per user request).
